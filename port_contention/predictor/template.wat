;; Template for port contention infer
(module
  (type (;0;) (func ))
  
    ;;the real version, 10000 iterations
   (func $main (export "main")
     ;; Here we inject the instruction 
   )
  (export "main" (func $main))
)